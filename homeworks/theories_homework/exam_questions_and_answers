# 大数据理论课考试题目及答案

## Q1 Hadoop有哪些主要缺点？为解决这些问题，数据湖与数据仓库分别采取了哪些改进措施？

### Hadoop 的主要缺点包括：
1. **执行效率低**  
   Hadoop MapReduce 是基于磁盘的批处理框架，缺乏向量化执行与内存优化，计算延迟高、吞吐低，不适用于交互式分析或实时处理。

2. **数据格式原始，缺乏灵活性**  
   HDFS 主要存储原始文件，未统一数据格式，不支持列式存储，压缩与查询效率较差。

3. **存储与计算耦合**  
   Hadoop 的计算资源（如 YARN）通常绑定在本地集群节点，资源调度灵活性差，难以弹性扩容或应对多样化负载。

4. **上层松散下层固化**  
   Hadoop 生态的松耦合设计虽然提供了灵活性，但也导致组件间协同效率低下，查询性能远不如深度集成的现代数据仓库。同时，在传统 Hadoop 架构中，许多组件（如 Hive、Spark SQL、Presto）都依赖 Hive Metastore（HMS）来获取元数据，这种对 HMS 的依赖使得底层与 HDFS 形成了强耦合关系，进一步抬高了系统的迁移成本，劣于采用开放架构与存算分离设计的数据湖方案。

5. **数据治理能力薄弱**  
   缺乏统一的元数据管理、安全策略和权限控制，不适合企业级数据管理需求。

### 数据仓库的应对策略：
- 列式存储 + 向量化执行：如 ClickHouse、Snowflake 使用列存格式（Parquet/ORC）+ 向量化计算，极大提升了分析性能。
- 紧耦合架构 + 全局优化器：将存储、计算、查询优化等模块深度整合，提高整体执行效率与一致性。
- 完整治理机制：内建元数据管理、权限控制、审计追踪等功能，适合 BI 报表、财务分析等结构化场景。

### 数据湖的应对策略：
- 存算分离架构：使用如 S3/HDFS 等对象存储保存数据，计算引擎（如 Spark、Flink、Presto）可以弹性调用资源，提升灵活性与可扩展性。
- 支持多格式 + 列式压缩：以 Parquet/ORC 为主的列式文件格式，提高存储效率与查询性能。
- 组件化生态体系：引入 Hive Metastore、Apache Iceberg、Delta Lake 等模块，实现元数据管理、事务控制、数据版本控制等功能，提升治理能力。

---

## Q2 请阅读以下业务场景，并判断在该场景中更适合采用 OLAP 架构还是 OLTP 架构，并说明原因

**场景描述：**  
某大型电商平台希望搭建一个系统，支持亿级商品浏览记录的分析与每小时生成一次实时运营报表，用于分析用户行为偏好、转化漏斗、热销品类变化等。

**问题要求：**  
请选择该场景更适合使用 OLAP 还是 OLTP 架构。请结合 OLAP 与 OLTP 的特点，说明你选择的理由

### 答案：
该场景更适合采用 OLAP 架构。

**原因：**
1. **业务目标为分析型操作**  
   该系统目的是分析用户行为、生成运营报表，不是处理日常交易或订单写入，属于典型的"分析型"任务。

2. **数据量级巨大**  
   涉及"亿级浏览记录"，数据量大，适合采用 OLAP 架构中的列式存储和压缩技术，提高查询效率。

3. **查询类型复杂**  
   需要支持转化漏斗分析、品类趋势、偏好挖掘等多维查询、聚合分析，正是 OLAP 擅长的场景。

4. **实时性为小时级**  
   OLAP 系统可通过流式数据管道（如 Kafka + ClickHouse 等）实现小时级近实时分析，满足需求。

5. **OLTP 不适合此场景**  
   OLTP 优势在于处理高并发、小事务、数据写入（如下单、支付），不适合大规模复杂查询和分析。

---

## Q3 比较数据湖与数据仓库在数据类型、成本、治理、安全、性能等方面的差异，并结合实际应用场景说明各自的优势

### 差异对比：
| 维度 | 数据湖 | 数据仓库 |
|------|--------|----------|
| **数据类型** | 支持结构化、半结构化和非结构化数据 | 主要处理结构化数据 |
| **成本** | 存储成本低（对象存储） | 计算资源成本较高 |
| **治理** | 治理机制相对薄弱，需额外工具 | 治理成熟，数据质量控制强 |
| **安全** | 依赖存储层安全机制 | 集成多种安全控制 |
| **性能** | 适合离线批量处理和数据探索 | 高性能，支持复杂查询和大并发 |

### 应用场景对比：
#### 数据湖优势场景：
> 某大型互联网公司需要收集 App 的用户点击流、日志文件、社交媒体内容及传感器数据，用于后续机器学习训练与行为建模。这类多源异构数据无需立即清洗，先进入数据湖保存最合适。

#### 数据仓库优势场景：
> 一家连锁零售企业每天需要生成销售日报、利润分析、库存预警，数据结构清晰且要求时效性和准确性，数据仓库能保障高效查询与可靠报表。

---

## Q4 “湖仓一体”架构相比于传统数据湖与数据仓库的方案，有哪些优势？请列举至少三点

### 优势：
1. **统一存储与计算引擎**  
   通过一套系统（如 Databricks、Snowflake）处理结构化与非结构化数据，简化架构。

2. **提升查询性能**  
   在保留数据湖低成本存储的基础上，引入加速索引、元数据优化等方式，提高 OLAP 查询性能。

3. **增强数据治理能力**  
   融合数据仓库的强治理能力，如数据版本控制、审计日志等，提升对企业级数据资产的管理能力。

4. **降低数据冗余与延迟**  
   在原始数据上直接进行分析，无需频繁将数据从湖搬运到仓。减少了数据复制和同步的成本，降低了数据延迟，更适合实时或准实时分析场景。

5. **支持多样化的数据应用场景**  
   兼容传统 BI 报表分析与现代 AI/ML 训练场景，既能服务于业务运营层的数据分析，也能支撑研发团队进行特征提取、模型训练等工作，实现一份数据多种用途，增强平台通用性。

---

## Q5 什么是“批流一体”？请分别说明“批处理”和“流处理”的定义，并说明为什么“批流一体”架构是现代数据系统的趋势

### 定义：
- **批流一体**  
  将批处理和流处理能力融合到一个统一的数据处理架构中，能够同时支持对历史大规模数据的离线批量处理和对实时数据的连续流式处理。

- **批处理**  
  针对大规模数据的定时、分批处理，通常用于离线计算、报表生成和数据清洗，处理延迟较高（分钟/小时级）。

- **流处理**  
  对实时产生的数据进行持续不断的处理，适用于监控、告警、实时推荐等场景，处理延迟低（秒级/毫秒级）。

### 成为趋势的原因：
1. **统一开发与运维**  
   避免为批和流分别维护两套系统，降低复杂度和成本。

2. **保证数据一致性**  
   通过同一套计算框架和存储体系，减少批流数据结果差异，提高分析准确性。

3. **满足多样化需求**  
   既支持历史数据深度分析，也能实现实时响应，提升业务敏捷性和实时决策能力。
